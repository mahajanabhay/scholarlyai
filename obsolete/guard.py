# LEGACY FILE — not imported by any route. Do not delete (referenced in git history).
# Do not run directly in production.

def study_guard(user_input: str) -> str:
    """Legacy stub — academic guard is now handled inline in chat_routes.py"""
    raise NotImplementedError("Use _is_obviously_non_academic() in chat_routes.py instead.")

if __name__ == "__main__":
    print("guard.py is a legacy stub. See chat_routes._is_obviously_non_academic()")